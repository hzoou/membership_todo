module.exports = {
    GET_BOARD_IDX_BY_USER_ID: 'SELECT BOARD.idx, BOARD.private FROM BOARD WHERE BOARD.USER_idx = (SELECT USER.idx FROM USER WHERE USER.id = ?)',
    GET_ALL_LIST_BY_BOARD: 'SELECT L.idx as LIST_idx, I.idx as ITEM_idx, L.title as LIST_title, I.title as ITEM_title FROM LIST L LEFT JOIN ITEM I ON I.LIST_idx = L.idx WHERE L.BOARD_idx = ? ORDER BY I.idx DESC',
    MAKE_BOARD: 'INSERT INTO BOARD (USER_idx) VALUES (?)',
    MAKE_LIST: 'INSERT INTO LIST (title, BOARD_idx) VALUES (?, ?)',
    UPDATE_LIST: 'UPDATE LIST SET title = ? WHERE idx = ?',
    INSERT_ITEM: 'INSERT INTO ITEM (title, LIST_idx) VALUES (?, ?)',
    DELETE_ITEM: 'DELETE FROM ITEM WHERE idx = ?',
    UPDATE_ITEM: 'UPDATE ITEM SET title = ? WHERE idx = ?',
    IS_AUTHORIZED_USER: 'SELECT * FROM PERMISSION WHERE USER_idx = ? AND BOARD_idx = ?',
    FIND_USER: 'SELECT * FROM USER WHERE id = ?',
    GET_ALL_USER: 'SELECT idx, id, admin FROM USER',
    GET_ALL_USER_EXCEPT_FOR_ME: 'SELECT id FROM USER WHERE !(id = ?)',
    INSERT_USER: 'INSERT INTO USER (id, pw) VALUES (?, ?)',
    UPDATE_USER: 'UPDATE USER SET admin = ? WHERE idx = ?',
    DELETE_USER: 'DELETE FROM USER WHERE idx = ?',
    UPDATE_PRIVATE_OF_BOARD: 'UPDATE BOARD SET private = ? WHERE idx = ?',
    GET_PERMISSION_OF_BOARD: 'SELECT board_idx, id, authentic FROM PERMISSION JOIN USER ON USER.idx = PERMISSION.USER_idx AND PERMISSION.BOARD_idx = ?',
    UPDATE_PERMISSION_OF_BOARD: 'UPDATE PERMISSION SET authentic = ? WHERE BOARD_idx = ? AND USER_idx = (SELECT idx FROM USER WHERE id = ?)',
    DELETE_PERMISSION_OF_BOARD: 'DELETE FROM PERMISSION WHERE BOARD_idx = ? AND USER_idx = (SELECT idx FROM USER WHERE id = ?)',
    INSERT_PERMISSION_OF_BOARD: 'INSERT INTO PERMISSION (authentic, BOARD_idx, USER_idx) VALUES (?, ?, (SELECT idx FROM USER WHERE id = ?))',
    GET_PERMISSION_OF_USER: 'SELECT U.id, P.authentic FROM PERMISSION P JOIN BOARD B JOIN USER U ON B.USER_idx = U.idx ON B.idx = P.BOARD_idx WHERE P.USER_idx = (SELECT idx FROM USER WHERE id = ?)',
};